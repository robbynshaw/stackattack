'use strict';

// Declare app level module which depends on views, and components
angular.module('stackattackApp', [
    // Dependencies
    'ngRoute',
    'core',
    'gameOverview',
    'questionDetail',
    'questionList',
    'recentQuestionList'
]);
